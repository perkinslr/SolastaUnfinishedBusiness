name: Compile
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
      with:
        repository: perkinslr/SolastaReferenceAssemblies
        ssh-key: ${{ secrets.REF_DEPLOY_KEY }}
        path: /home/runner/work/SolastaUnfinishedBusiness/SRA
    - uses: actions/checkout@v3
    - name: restore
      run: |
        SolastaInstallDir=/home/runner/work/SolastaUnfinishedBusiness/SRA SolutionDir=$PWD dotnet restore SolastaUnfinishedBusiness.sln
    - name: clean
      run: |
        SolastaInstallDir=/home/runner/work/SolastaUnfinishedBusiness/SRA SolutionDir=$PWD dotnet clean SolastaUnfinishedBusiness.sln  
    - name: build
      run: |
        SolastaInstallDir=/home/runner/work/SolastaUnfinishedBusiness/SRA SolutionDir=$PWD dotnet build SolastaUnfinishedBusiness.sln
        
    - uses: actions/upload-artifact@v4
      with:
        name: UnfinishedBusiness
        path: SolastaReferenceAssemblies/Mods
        
